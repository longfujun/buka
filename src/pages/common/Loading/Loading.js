import React from 'react'
import './style.scss'

const Loading = (props)=>{
    return (
        <div className="page subpage" id="loading">
            <h1>正在加载中...</h1>
        </div>
    )
}

export default Loading;